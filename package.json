{
  "name": "project-1",
  "version": "1.0.0",
  "description": "Project 1",
  "scripts": {
    "ui:install": "cd ui && npm install",
    "ui:build": "cd ui && npm run build",
    "server:install": "cd server && npm install",
    "start": "npm run ui:build && node server/server.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
